var node = require('./node2.jsx');
var React = require('react');
var ReactDOM = require('react-dom');

console.log(node.ComponentNode);

ReactDOM.render(<ComponentNode />, document.getElementById('app'));
